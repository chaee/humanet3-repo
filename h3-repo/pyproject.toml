[tool.poetry]
name = "h3-repo"
version = "0.1.0"
description = ""
authors = ["Yun <yun@mpib-berlin.mpg.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
